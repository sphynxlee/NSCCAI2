import { createContext } from "react";

// const [theme, setTheme] = useState("commodore 64");
// export const ThemeContext = createContext([theme, setTheme]);

export const ThemeContext = createContext("gibberish");